= render template: 'squibble_base_admin/application/index'

-#

  - content_for :page_head do
    - cache [ 'admin/index/page_head', resource_class, collection.total_count ] do
      %h1
        = translated_resource_class(resource_class, collection.total_count)
        %small= page_entries_info collection

  - content_for :page_subheading do
    .row.hidden-print.margin-bottom-15
      .col-md-8
        = render 'admin/index_actions', actions: @actions_index, resource_class: resource_class, current_admin_user: current_admin_user, current_principal: current_principal

<<<<<<< HEAD
      .col-md-4
        = render 'squibble_base_admin/application/index/filter', { current_index_filters: current_index_filters }
        = render 'squibble_base_admin/application/index/search'

  - cache index_collection_cache_key_with_pagination( resource_class, collection, current_admin_user: current_admin_user) do
    - breadcrumb index_breadcrumb

    %section#sq_collection.admin_index{ class: [ resource_class.to_s.parameterize ] }
      - if collection.empty?
        = render 'admin/index_collection_empty', resource_class: resource_class, params: params
      - else
        = render partial: list_partial_type.to_s, locals: { collection: collection }
        = paginate collection, remote: true
=======
  %section#sq_collection.admin_index{ class: [ resource_class.to_s.parameterize ] }
    - if collection.empty?
      = render 'admin/index_collection_empty', resource_class: resource_class, params: params
    - else
      = render partial: list_partial_type.to_s, locals: { collection: collection }
      = paginate collection
>>>>>>> ab338be42b743f52373c9d8181c3f1ccdfc3b39b
