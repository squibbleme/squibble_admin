- cache ['helpers/squibble_admin/admin/markup/show_helper/preview_show', resource, preview_path ] do
  = portlet do
    = portlet_title do
      = portlet_title_caption resource_attribute_name(:preview.class)
    = portlet_body do

      %div{ data: { path: resource.preview.path, url: resource.preview.url } }
        - if resource.preview_content_type.present? && resource.preview_content_type.starts_with?('image')
          = sq_image_responsive_tag preview_path
          %hr

        = show.attribute :preview_file_name
        = show.attribute :preview_file_size do
          = number_to_human_size resource.preview_file_size
        = show.attribute :preview_content_type
        = show.attribute :preview_extension
